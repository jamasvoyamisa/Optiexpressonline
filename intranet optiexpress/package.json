{
  "scripts": {
    "build": "rm -rf dist && mkdir -p dist && cp .htaccess robots.txt aurora.js avisos.html catalogos.html catalogos.js index.html promociones-activas.html script.js styles.css dist/ && cp -R images dist/"
  },
  "dependencies": {
    "ogl": "^1.0.11"
  }
}
